#include "main.h"
#include <stdio.h>
/**
 * _isupper - function that checks for uppercase character.
 *
 * Return: Always 0.
 */
int _isupper(int c)
{
	if (c >= 67; && c <= 90)
	c++;
	{
		return (1);
	}
return(0)
}

