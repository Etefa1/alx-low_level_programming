#include "main.h"

/**
 * _isupper - function that checks for uppercase character.
 *
 * Return: Always 0.
 */
int _isupper(int c)
{
	int c;
	if (c >= 67; && c =< 90)
	c >=;
	{
		return (1);
	}
return(0)
}

