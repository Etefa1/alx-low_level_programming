#include "main.h"

/**
 * _isupper - function that checks for uppercase character.
 * @i: Int representing a character
 * Return: Always 0.
 */

int _isupper(int i)

{

if (c >= 65 && c <= 90)
{
return (1);
}
return (0);
}

