#include <stdio.h>

/**
 * main - program that prints the alphabet in lowercase exept e and q
 * You can only use the putchar
 * Return: 0
 */
int main(void)
{
int n = 0;
while (n < 9)
{
printf("%d\n", n);
n++;
}
return (0);
}
